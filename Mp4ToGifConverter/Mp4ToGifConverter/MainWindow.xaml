<Window x:Class="Mp4ToGifConverter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Mp4ToGifConverter"
        xmlns:vm="clr-namespace:Mp4ToGifConverter.ViewModels"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        d:DataContext="{d:DesignInstance vm:MainWindowViewModel, IsDesignTimeCreatable=False}"
        mc:Ignorable="d"
        ThemeMode="System"
        Title="MainWindow" Height="450" Width="800">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding MainWindowLoadedCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0" Content="Select File" Margin="0,0,0,10" Command="{Binding SelectInputCommand}"/>
        <TextBlock Grid.Row="1" Text="{Binding InputPathText}" Margin="0,0,0,10" TextWrapping="Wrap" />

        <Button Grid.Row="2" Content="Select Output Path" Margin="0,0,0,10" Command="{Binding SeletOutputCommand}"/>
        <TextBlock Grid.Row="3" Text="{Binding OutputPath}" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="FPS:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Text="{Binding Fps, Mode=TwoWay}" Width="150"/>
            <TextBlock Text="Scale:" VerticalAlignment="Center" Margin="20,0,10,0"/>
            <TextBox Text="{Binding Scale, Mode=TwoWay}" Width="150"/>
            <CheckBox Content="Grey Scale" Margin="20,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <Button Grid.Row="5" Content="Convert"/>

        <ProgressBar Grid.Row="6" Height="20" Margin="0,0,10,0" Visibility="{Binding IsInProgress}"/>
        <TextBlock Grid.Row="6" VerticalAlignment="Center" HorizontalAlignment="Center"/>
    </Grid>
</Window>
